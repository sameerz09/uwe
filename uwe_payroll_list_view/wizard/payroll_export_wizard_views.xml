<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_payroll_export_wizard_form" model="ir.ui.view">
            <field name="name">payroll.export.wizard.form</field>
            <field name="model">payroll.export.wizard</field>
            <field name="arch" type="xml">
                <form string="Export Payslips to Excel">
                    <group>
                        <field name="export_type" widget="radio"/>
                        <field name="payslip_ids" widget="many2many_tags" 
                               attrs="{'invisible': [('export_type', '!=', 'selected')]}"/>
                    </group>
                    <group>
                        <field name="file_name"/>
                    </group>
                    <footer>
                        <button name="action_export_excel" type="object" 
                                string="Export to Excel" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_payroll_export_wizard" model="ir.actions.act_window">
            <field name="name">Export Payslips to Excel</field>
            <field name="res_model">payroll.export.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
