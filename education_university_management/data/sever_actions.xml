<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Server Action to Send Email with Degree Report -->
    <record id="action_send_degree_report_email" model="ir.actions.server">
        <field name="name">Send Degree Report via Email</field>
        <field name="model_id" ref="education_university_management.model_university_degree_result"/>
        <field name="binding_model_id" ref="education_university_management.model_university_degree_result"/>
        <field name="state">code</field>
        <field name="code">
          action = records.action_send_degree()
        </field>
    </record>


</odoo>